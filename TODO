* Move movable.canMove(dir) and movable.move(dir)
  to controller and make it canMove(movable, dir) and
  move(movable, dir)

* Make other.move generate a new MoveEvent for concistency.

* Unit tests.

* Wall detection. Border detection.

* Gui.

* Keyboard control. Mouse swipe support.

* Level editor.

* A shitload of levels.

* Levels JSON backend.

